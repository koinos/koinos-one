# Knodel Codex Project Memory

Last updated: 2026-06-01

This file is the local project memory for Codex work in this repo. Codex Desktop does not guarantee that arbitrary repo files are always read automatically in every thread, so explicitly ask to read `AGENTS.md` when resuming if the context was compacted or lost.

## Project Mission

Act as an expert blockchain engineer for this project. The goal is to build an optimized Koinos block-producing node that is easy for end users to launch and operate. The current primary target is macOS; Windows optimization will follow. All implementation decisions must preserve compatibility with the Koinos protocol and avoid shortcuts that would diverge from mainnet behavior.

All project documentation must be written in English, even when discussion with the user happens in Spanish.

## Current Monolith Status

- Gates A, B, C and D are complete for the monolithic migration roadmap.
- Gate E is implemented in code: Knodel no longer forces monolith `p2p` off by default, presets keep core monolith components enabled, and the monolith can be smoked locally with `p2p` plus `jsonrpc`.
- Gate F has an executable soak harness and report generator. A 5h production-style soak was attempted on 2026-05-24, but it was aborted as invalid because the node stayed at `0` connected peers and head height `0`. Follow-up runs proved cpp-libp2p can connect to mainnet peers, complete Koinos handshake, and apply the first `500` blocks when a public peer accepts Peer RPC, but sustained sync is not signed off yet. The local C++ side now advertises `koinos/p2p/1.0.0`, runs libp2p on one IO runner, owns seed dialing in `P2PNode` with paced attempts, avoids duplicate in-flight dials, serves incoming Peer RPC (`GetChainID`, `GetHeadBlock`, `GetAncestorBlockID`, `GetBlocks`) from local `chain`/`block_store`, disconnects stale EOF/reset peers without score-threshold escalation, serializes sync block application, and ignores local "prior to irreversibility" races from competing peers. `scripts/mainnet-peer-candidates.txt` now prioritizes the legacy `config-example` seeds and keeps production-log peers as fallbacks. Latest Go probe from this machine failed against all five official seeds during security negotiation/timeout/refused, and the only peers accepting raw libp2p returned `protocol_version=unknown` and rejected Koinos Peer RPC. Treat the latest failure as peer availability/backoff until a fresh Go probe finds a Peer RPC-capable target.
- Latest Gate F retry on 2026-05-25 found one transient Peer RPC-capable Go target, `/ip4/94.130.148.114/tcp/8888/p2p/QmQ841mUuYeCtbZXdEMeKcYCx4CZydgz84zSDqWVCeJ4H8`, at head height `36232881`, but immediate repeat probes failed with EOF/reset and the C++ `300s` short soak against that peer was invalid: the monolith started cleanly, JSON-RPC remained available, but no peer session, handshake, sync rows, or head progress appeared before the `240s` startup grace expired. `scripts/probe-mainnet-seeds.sh` now resolves relative `SEED_PROBE_OUTPUT` paths from the repo root before changing into the `koinos-p2p` submodule. Gate F is a mainnet readiness gate, not a blocker for continued monolith implementation. `scripts/ab-mainnet-peer-acquisition.sh` now runs a Go legacy direct-dial Peer RPC discovery/stability baseline before a C++ monolith soak; the first report-only run against the current mainnet candidate list found `0` Go Peer RPC-capable targets and recorded `blocked: go-discovery-found-no-peer-rpc-targets` in `docs/roadmap/monolith/networking/MONOLITH_AB_PEER_ACQUISITION_REPORT.md`.
- Sprint 1.1 real backup restore validation now passes on the external SSD path `/Volumes/external/knodel-monolith-restore`: the seed backup is legacy, `chain/blockchain` is reused, and `block_store/db` is converted from Badger to monolith RocksDB `basedir/db`.
- Sprint 1.2 JSON-RPC parity now passes against the local legacy Go stack on the restored dataset: 21/21 cases matched after canonicalizing byte encodings.
- Sprint 1.3 mempool end-to-end is locally complete. `chain.submit_transaction` now has the in-process mempool RPC coverage it requires (`check_pending_account_resources`, `check_account_nonce`, `get_pending_nonce`, and `get_pending_transaction_count`), accepted transactions flow from `koinos.transaction.accept` into the real mempool implementation through the monolith event bus, and the default 120s transaction expiration path is covered by focused C++ tests.
- Sprint 2 backend implementation is complete in the monolith: the block producer loads WIF keys, writes `block_producer/public.key`, assembles resource-bounded blocks, signs `federated` and PoB blocks, resolves PoB/VHP chain data, prunes failed transactions and retries, and gates production on P2P gossip readiness by default. Local build and CTest coverage pass. Private three-node federated validation passes: `producer-1` produced a signed block, `observer-1` synced it over P2P from the producer, and observer block store, transaction store, and contract meta store health checks passed. Private three-node PoB validation also passes with `PRIVATE_TESTNET_MODE=pob`: the deterministic runtime genesis includes staged `koin`, `name_service`, `pob`, and `vhp` system-contract bytecode, exact system-call dispatch keys, name-service records, producer hot-key registration, KOIN/RC balance, effective VHP, canonical PoB metadata difficulty, and the producer-to-PoB VHP burn allowance. The 30-minute private PoB soak passed on 2026-05-24: 31 samples, zero stalled samples, zero severe log matches, final producer head `74362`, final observer head `73842`, and final observer block-store height `73842`. Isolated seed-host external validation also passed on 2026-05-24 at commit `ec1d737`: the branch was pulled into `/mnt/HC_Volume_105581636/knodel-external-signoff/knodel` on `seed.koinosfoundation.org`, cpp-libp2p `koinos_node` was built on the external volume, and a three-node private PoB soak ran for `300s` on high localhost ports without touching the production Docker stack. Final external heads were producer `8736`, observer `8384`, observer block-store `8384`, with `0` stalled samples, `0` severe log matches, transaction/contract-meta checks `ok`, clean shutdown, and no lingering external `koinos_node` processes.
- A live external Koinos Foundation testnet is now available and documented in `pgarciagon/koinos_testnet`: JSON-RPC `https://testnet.koinosfoundation.org/jsonrpc`, compatibility root `https://testnet.koinosfoundation.org/`, public P2P `testnet.koinosfoundation.org:8888`, peer ID `QmYV414G6xRzkSUytntEsBsCSjXrVGubfYJn4vpeER2s2W`, and chain ID `EiAIKVvm6-V2qmsmUvPJy09vCCLbtn9lHFpwrJbcTIEWRQ==`. On 2026-05-25, `/health` returned `ok`, `chain.get_head_info` returned a live head, public TCP `8888` was reachable, a libp2p-only public dial succeeded after clearing a temporary source-IP p2p error score, and a non-producing monolith observer completed Koinos handshake and synced from height `0` to `4000` in about `54s` with no score-threshold rows. Treat this as the external validation target for Sprint 2.4.
- `kcli` is installed locally at `/opt/homebrew/bin/kcli` and configured through `/Users/pgarcgo/.kcli/config.json` with default network `testnet` and RPC `https://testnet.koinosfoundation.org/jsonrpc`. It should be used as the Koinos wallet/client for live testnet operations: generate/import the producer-control wallet, request faucet vKOIN, check balances, burn KOIN to VHP, register the monolith producer public key, and inspect producer status.
- Live testnet producer validation is complete through accepted public-testnet block production. A dedicated producer-control wallet was generated with `kcli`; the active control address is `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`. The previous `kcli` wallet/config were backed up under `/Users/pgarcgo/.kcli/backups`. The user faucet-funded the account, and burns brought it to `2,850,095` effective VHP while leaving `5` KOIN liquid: initial `95` KOIN burn tx `0x1220b4954d9d4b5be350326a4a246d6ee4cdfa7f4ca12a379b68dc205053ed434ff3` at block `5248232`, later `350,000` KOIN burn tx `0x12203c7ac43e032ed02d614fecb41518265b6ae5da8e9d537fe16d3582364bb4108b` at block `5377934`, and `2,500,000` KOIN burn tx `0x1220081f1e69449f04ffdbb333ed82ff34435947196b9844527f11cdd65d1eb188a4` at block `5378193`. A separate monolith hot key was generated under `/Users/pgarcgo/.kcli/knodel-testnet-producer` with public key `AjyRoy9QlZP-AuojYV-cBlHC64mP-ZliaibjmjVnHL97` and hot-key address `1FFpnG9ev9ZsZ91uDWF19ro7zaKodcBtzX`; private material and the generated wallet password are only in local `0600` files outside the repo. The public key was registered for the producer-control address in tx `0x1220ddc565480f6689f11735e9e2dd5b1add9949bb373e63ecba8e48eb148eb62505`, confirmed at block `5248344`, and `kcli get-producer-key` returns the expected 33-byte key. The prepared basedir `/Users/pgarcgo/.kcli/knodel-testnet-producer/basedir` uses the live testnet genesis/descriptors and has `block_producer.producer: 1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`. A first catch-up pass exposed that far-behind nodes could score a seed for future gossiped blocks with unknown parents; `p2p_node.cpp` now ignores future gossiped blocks while local head is more than one block behind the gossiped height, and `koinos_p2p_one_peer_sync_test` covers that behavior. A long non-producing catch-up later reached height `4649031` but crashed with a RocksDB write exception when the internal macOS data volume had only `1.9 GiB` free. On 2026-05-30 the basedir was moved to `/Volumes/external/knodel-testnet-producer/basedir` and the original path was replaced with a symlink. The restarted observer caught up to live testnet head `5376554`, then was cleanly stopped and restarted as a VHP-staked monolith producer with `features.block_producer: true`. The first producer window stayed healthy but produced no block despite high VHP. Root cause was a stale PoB candidate cache: `BlockProducer` kept a candidate block after the local head advanced, so a later winning VRF could propose against an obsolete `previous`. `block_producer.cpp` now rebuilds the PoB bundle when the local head changes, and `koinos_block_producer_test` covers that path. After rebuilding and restarting at `2026-05-30T12:10:08Z`, the monolith immediately produced public-testnet blocks accepted by `https://testnet.koinosfoundation.org/jsonrpc`; examples include heights `5378368` and `5378369`, both signed by `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`, with local and remote heads matching at `5378374`.
- A post-signoff producer soak exposed a second live-fork issue: after accepted production, the node disconnected from the single testnet seed with `checkpoint mismatch` and stopped at height `5378534`. The local block at that height matched public RPC, so this was not local corruption; it was the C++ sync loop treating a volatile head fork as a peer fault. Legacy `koinos-p2p` anchors live sync on the local LIB, not the current head. `P2PNode::peer_handshake` no longer validates peer ancestry against the volatile local head, and `P2PNode::request_sync_blocks` now checks that the peer contains the local LIB, skips requests when the peer head is already known locally, and fetches from `LIB+1`. `koinos_p2p_one_peer_sync_test` now covers a local-head/peer-head fork above the same LIB without disconnecting. After rebuilding and restarting at `2026-05-30T12:26:38Z`, the producer reconnected, caught up from the LIB anchor, reopened the gossip gate, and produced accepted block `5378792` (`0x1220d2a5bdd51fe1de9b25fb798f851ca1006214db9e2fb66f4d48207d1764e3aee5`) signed by `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`; local and public RPC matched at head `5378802`, with the reporter showing `warnings=0` and `score threshold rows=0`.
- Follow-up producer soak check at `2026-05-30T15:19:04Z`: the live monolith producer was still running as PID `32977`, guarded by `caffeinate -dims -w 32977`, connected to `/dns4/testnet.koinosfoundation.org/tcp/8888/p2p/QmYV414G6xRzkSUytntEsBsCSjXrVGubfYJn4vpeER2s2W`, and local JSON-RPC matched public RPC at head `5382991` with the same block id, LIB, state merkle root, and block time. The recent log window contained `126` produced-block rows and `0` warning, score-threshold, checkpoint-mismatch, or gossip-gate rows. External SSD free space was about `19 GiB`, above the current `10 GiB` stop floor but close enough to keep monitoring during longer soaks.
- Longer live-producer soak checkpoint at `2026-05-30T23:19:05Z`: the current keep-alive producer process had been running for more than `10h`, with `caffeinate -dims -w 32977` still active. Local JSON-RPC and public RPC matched at head `5392416` with the same block id and LIB. The checked log window contained `778` produced-block rows and `552` public-seed peer rows. There were `0` warning, score-threshold, checkpoint-mismatch, or gossip-gate rows. External SSD free space was `18.78 GiB`, still above the `10 GiB` stop floor.
- Live-producer soak check during Sprint 4.3 work at `2026-05-31T20:51Z`: the live producer was left running and no producer-key registration was submitted. PID `32977` had been up for more than `1d7h`, `caffeinate -dims -w 32977` was active, local JSON-RPC and public RPC matched at head `5417632` with the same block id and LIB, recent produced-block rows were present, and `/Volumes/external` had about `19 GiB` free.
- Sprint 4.3 final live-producer check at `2026-05-31T21:58Z`: the live producer was still running as PID `32977`, guarded by `caffeinate -dims -w 32977`; local JSON-RPC and public RPC matched at head `5418907`, and `/Volumes/external` still had about `19 GiB` free. The gRPC work did not restart the producer and did not submit any producer-key registration.
- Live-producer 48h soak milestone at `2026-06-01T13:46:44Z`: the same producer process `32977` had been running for more than `2d`, still guarded by `caffeinate -dims -w 32977`. Local JSON-RPC and public RPC matched at head `5437363` with the same block id and LIB. The rolling log window showed recent produced blocks through local produced-block count `#23027`, continuous public-seed peer snapshots for `/dns4/testnet.koinosfoundation.org/tcp/8888/p2p/QmYV414G6xRzkSUytntEsBsCSjXrVGubfYJn4vpeER2s2W`, and no warning, score-threshold, checkpoint-mismatch, or gossip-gate rows. `/Volumes/external` had about `18.5 GiB` free, still above the `10 GiB` stop floor.
- Live `kcli` transaction validation through the monolith JSON-RPC is complete. To make `kcli` work against `http://127.0.0.1:18122`, `jsonrpc_server.cpp` now normalizes public Koinos JSON encodings on input (Base58 addresses, `0x` ids/hashes, websafe byte fields) and returns websafe/base64url bytes for `chain.get_chain_id`, `chain.get_head_info`, `chain.get_account_nonce`, and `chain.read_contract`; the HTTP server also handles repeated keep-alive requests on the same socket instead of closing after one request. A live `kcli transfer` from producer-control `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi` to hot key `1FFpnG9ev9ZsZ91uDWF19ro7zaKodcBtzX` submitted tx `0x12209ffc21122d2dfd36b01be685bb88a6867b8325c350df5312714b7f316e0f27cb`, confirmed in public-testnet block `5380195`; the local monolith log shows that the monolith producer produced that block with `1 transaction`, public `kcli block 5380195` shows the same tx id, and public balance checks showed the hot key at `0.01` KOIN. Focused CTest passed after the change for `koinos_p2p_one_peer_sync_test` and `koinos_block_producer_test`.
- A first external legacy observer has been added to the live testnet on VPS1 `46.225.170.6` under `/opt/koinos-testnet-legacy-observer`. It runs the legacy Docker microservice observer stack with `block_producer` disabled, JSON-RPC bound to `127.0.0.1:18080`, and public P2P exposed on `46.225.170.6:28888`. UFW allows `28888/tcp`. The P2P identity was made persistent before relying on it as a peer: `/ip4/46.225.170.6/tcp/28888/p2p/QmeJnbWzRZ91zgTDTxs1UdsbRFFM6B26PntLdk69N63ePY`. On 2026-05-30, `scripts/probe-mainnet-seeds.sh` verified raw libp2p plus Peer RPC against that address with protocol `koinos/p2p/1.0.0`, the expected testnet chain ID, and observer head `8253`; a follow-up JSON-RPC check showed continued catch-up to height `12775`. The observer is still catching up from genesis, so do not use it as an independent near-head acceptance witness until it reaches the live head.
- A second external legacy observer has been added on VM2 `46.62.155.105` under `/opt/koinos-testnet-legacy-observer`. VM2 is smaller (`2` vCPU, `4 GiB` RAM, about `29 GiB` free at setup), so it is intentionally a non-producing legacy observer, not a monolith build host. It runs the same Docker microservice observer stack with JSON-RPC bound to `127.0.0.1:18080` and public P2P mapped to `46.62.155.105:28888`; UFW is inactive. The persistent P2P identity is `/ip4/46.62.155.105/tcp/28888/p2p/QmXfSaJjSPSivJURC9RrCGKGmKtB3EA3AWEUksY2e189R3`. On 2026-05-30 it connected to the public testnet seed and advanced from height `23` to `6194`. External TCP `28888` is reachable, and the first external probe reached Peer RPC `GetChainID`, but `GetHeadBlock` reset while the node was under heavy genesis catch-up; later probes from this Mac then hit security resets, consistent with temporary source-IP scoring. Do not count VM2 as a fully Peer RPC-validated external peer until a later probe passes.
- Sprint 4 data migration has started. `vendor/koinos/koinos-node/tools/migrate_block_store.sh` is now a real offline migration wrapper instead of a placeholder: it streams legacy Badger `block_store/db` through `tools/legacy-block-store-exporter` into `import_block_store_stream`, writes monolith RocksDB `blocks`/`block_meta` column families, preserves embedded skip-list pointers byte-for-byte, compares exported/imported record and byte counts, and records `.monolith-migrated`. It also supports SHA-256 verification: `--verify sample` hashes sampled source records and target RocksDB readback records, while `--verify full` hashes every block-store record. A dry run against `/Volumes/external/knodel-monolith-restore/basedir` passed and reported the expected space warning: `41.3 GiB` legacy source, `28.3 GiB` free, below the conservative `2x` recommendation. Migrated-basedir node verification is now built into the wrapper through `--verify-node` and `--verify-node-only`; the smoke passed against `/Volumes/external/knodel-monolith-restore/basedir` with P2P disabled, JSON-RPC port `18113`, and `chain.get_head_info` returned height `36180957`.
- Sprint 4.3 gRPC compatibility now has strict legacy-vs-monolith and client compatibility evidence. The monolith registers the generated `koinos.services.koinos` gRPC service instead of the previous generic/envelope-path server, routes typed protobuf methods into the in-process chain, block store, mempool, contract meta store, transaction store, and account history services, returns `UNAVAILABLE` for disabled optional stores, maps `expected field ... was nil` validation errors to gRPC `INVALID_ARGUMENT`, keeps unexpected service exceptions as `INTERNAL`, and serves `get_gossip_status` from the monolith gossip gate state. `koinos_grpc_server_test` covers routing, protobuf responses, disabled-service errors, invalid-request errors, gossip-status response, and unexpected exceptions. `scripts/compare-grpc-parity.sh` plus `koinos_grpc_parity_probe` records gRPC status codes, protobuf JSON, and serialized response bytes, and can compare saved legacy/monolith result files with `GRPC_PARITY_LEGACY_INPUT` and `GRPC_PARITY_MONOLITH_INPUT` for datasets that cannot be opened by both stacks simultaneously. Sequential restored-data comparison used legacy gRPC `127.0.0.1:50052` and temporary monolith gRPC `127.0.0.1:50151`; 8/10 cases matched exactly. The two documented differences are legacy AMQP timeouts for missing `contract_id` and absent P2P `get_gossip_status` versus deterministic monolith gRPC responses (`INVALID_ARGUMENT` and `enabled=false`). Evidence: `/private/tmp/knodel-grpc-legacy-baseline-20260531T214846Z.json`, `/private/tmp/knodel-grpc-monolith-probe-20260531T215543Z.json`, `/private/tmp/knodel-grpc-file-comparison-20260531T215543Z.json`. `scripts/validate-grpc-client-compatibility.sh` now runs the generated probe, optional `grpcurl` typed-client calls, and audits local `koinos-cli`/`koinosctl`. On 2026-06-01, `grpcurl` successfully listed and called `koinos.services.koinos`; installed `koinos-cli v0.3.1` was confirmed to be HTTP JSON-RPC-only and passed a JSON-RPC smoke while failing as expected against the gRPC port; `koinosctl` was not installed. Evidence: `/private/tmp/knodel-grpc-client-validation-script-20260601T051041Z.txt` and `/private/tmp/knodel-grpc-client-validation-script-20260601T051041Z-probe.json`. Remaining work: re-run against `koinosctl` if that binary is added to the local toolchain.
- Sprint 4.4 is complete for the selected `koinos/koinos-integration-tests` functional regression bridge. `scripts/fetch-koinos-integration-tests.sh` pins upstream commit `74b64d739a98045630cb61557e1f141c04cd1eb1`; `scripts/run-koinos-integration-compat.sh inventory` records topology/safety metadata; Docker legacy mode remains available but optional. The native legacy runner starts GarageMQ plus the native legacy microservices on macOS and passes the high-value single-node upstream tests unchanged: `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `transaction_error`, `koin`, `vhp`, `pob`, and `propose_block`. The monolith runner passes the public JSON-RPC-compatible selected suite: `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `koin`, `vhp`, and `pob`; `koin` is adapted only at client construction from legacy AMQP to public JSON-RPC while preserving the upstream contract assertions. Direct AMQP-only upstream tests `transaction_error` and `propose_block` are intentionally kept as native-legacy baselines rather than monolith tests because the monolith does not expose AMQP as a public compatibility surface; replacement monolith coverage is Sprint 4.3 gRPC protobuf envelope/error parity, the block producer and mempool CTest suites, upstream `publish_transaction`/`pob`, and live-testnet transaction inclusion. During 4.4, `jsonrpc_server.cpp` gained recursive response byte-field rewriting from protobuf `koinos.btype` descriptors and recursive request normalization for `submit_block`/`propose_block` blocks so upstream Koinos Go JSON clients can round-trip block and transaction IDs correctly.
- The roadmap source of truth is `docs/roadmap/monolith/core/MONOLITH_PRODUCTION_PLAN.md`.
- The latest detailed Gate E/F report is `docs/roadmap/monolith/networking/MONOLITH_GATE_EF_REPORT.md`.
- The latest backup restore report is `docs/roadmap/monolith/backup-restore/MONOLITH_BACKUP_RESTORE_REPORT.md`.
- The latest JSON-RPC parity report is `docs/roadmap/monolith/core/MONOLITH_JSONRPC_PARITY_REPORT.md`.
- The latest private testnet report is `docs/roadmap/monolith/testnets/MONOLITH_PRIVATE_TESTNET_REPORT.md`.
- The latest external/shared testnet report is `docs/roadmap/monolith/testnets/MONOLITH_EXTERNAL_TESTNET_REPORT.md`.

## What Works Now

- `vendor/koinos/koinos-node/build/koinos_node` builds with `cpp-libp2p`.
- JSON-RPC responds from the monolith with local config data and is now compatible with `kcli`/koilib public JSON conventions for the live transfer path, including Base58/hex request normalization, websafe byte responses, and HTTP keep-alive reuse.
- Peer RPC compatibility is covered by Go-generated fixtures and live Go peer interop.
- One-peer sync is covered by controlled C++/Go live tests and applies blocks by RPC.
- GossipSub interop is covered both directions for blocks and transactions against a controlled Go peer.
- Knodel monolith mode starts one `koinos_node` process, derives component health from monolith logs, filters component logs, supports presets/restart, and has visible fallback when monolith startup fails.
- P2P peer snapshots are logged by the monolith as `[p2p] Connected peers:` rows so Knodel dashboard parsing can consume them.
- Real seed backup restore works through the monolith layout: converted import result was `records=40278912 blocks=40278911 meta=1 bytes=83883179053`; `chain.get_head_info` returned height `36180898` from the restored data.
- JSON-RPC parity harness covers 21 methods across chain, block_store, mempool, contract_meta_store, transaction_store, and account_history. It captures legacy baseline from `http://127.0.0.1:18084/` and compares monolith `http://127.0.0.1:18083/`.
- The `koinos/koinos-integration-tests` compatibility runner is in place. Native legacy mode passes the selected high-value single-node baseline without Docker, and monolith mode passes the selected public JSON-RPC suite: `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `koin`, `vhp`, and `pob`. JSON-RPC output rewrites protobuf bytes using `koinos.btype`, and JSON-RPC input normalizes full block payloads for `submit_block`/`propose_block`.
- gRPC compatibility now uses the public typed protobuf service `koinos.services.koinos`. `GRPCServer` routes generated service methods directly to monolith component APIs, exposes `get_gossip_status`, maps validation errors to `INVALID_ARGUMENT`, and `scripts/compare-grpc-parity.sh` can run monolith-only smoke probes, legacy-vs-monolith endpoint comparisons, or sequential saved-file comparisons through generated protobuf stubs.
- Mempool end-to-end wiring is in place for the monolith. Accepted transactions are added to the real mempool, `mempool.get_pending_transactions` returns them, pending RC reservations affect `check_pending_account_resources`, nonce duplication/next-nonce checks work through the same in-process RPC path used by chain, and `mempool.transaction-expiration` drives a 1s prune loop.
- Mainnet P2P no longer has a pure local timing blocker: seed dialing is paced like legacy, duplicate dials are suppressed, and the monolith can answer remote Peer RPC. The latest validation proves local build/tests pass and Go itself cannot currently find a Peer RPC-capable public target from this host. `scripts/probe-mainnet-seeds.sh` now treats raw libp2p dial success as insufficient by default: `OK` requires Koinos Peer RPC `GetChainID` and `GetHeadBlock`.
- Block producer backend now submits real populated blocks instead of empty proposals. `koinos_block_producer_test` verifies local federated block assembly, resource-bounded transaction inclusion, accepted receipt handling, and chain-reported failed transaction prune/retry behavior.
- Private Sprint 2 federated and PoB smokes work with three local monolith nodes. The harness patches a temporary Harbinger genesis for federated mode and generates a deterministic system-contract PoB genesis for PoB mode, forces gossip for private bootstrap, starts seed/producer/observer, connects the observer directly to both seed and producer, verifies observer head/block-store height, checks transaction/contract-meta service health, and supports duration-based private PoB soaks through `SOAK_DURATION_SECONDS`.
- Public testnet JSON-RPC and P2P are reachable. A non-producing monolith observer against `testnet.koinosfoundation.org` completed the first live observer sync check, and the `kcli` producer-control wallet setup, VHP burn, producer-key registration, full catch-up to the live head, controlled switch to `features.block_producer: true`, accepted live PoB block production, and a real `kcli transfer` through the monolith JSON-RPC are complete. Public RPC verified produced blocks at heights `5378368`, `5378369`, `5378792`, and transaction block `5380195` signed/produced by the configured producer address. The live sync path now follows legacy behavior by anchoring peer branch checks on the local LIB rather than the volatile local head, so normal competing live forks above LIB do not cause seed score-threshold disconnects. Use `SEED_PROBE_PEER_RPC=0` for a raw public P2P exposure check; the lightweight Go probe is not a full Koinos peer and repeated Peer RPC probes can temporarily trigger the Go p2p source-IP error scorer on the single-node testnet.
- VPS1 legacy observer is live and externally reachable at `/ip4/46.225.170.6/tcp/28888/p2p/QmeJnbWzRZ91zgTDTxs1UdsbRFFM6B26PntLdk69N63ePY`. It has correct chain ID and Peer RPC, connects to `testnet.koinosfoundation.org`, and is syncing from genesis through the legacy stack. Treat it as a topology/stability peer now and as an independent observer only after catch-up completes.
- VM2 legacy observer is live at `/ip4/46.62.155.105/tcp/28888/p2p/QmXfSaJjSPSivJURC9RrCGKGmKtB3EA3AWEUksY2e189R3` and syncing from genesis. Treat it as a syncing external observer only; full Peer RPC validation is still pending because `GetHeadBlock` reset during initial catch-up load.
- `kcli` testnet client setup is complete: current local version `1.4.0`, default network `testnet`, RPC `https://testnet.koinosfoundation.org/jsonrpc`; `kcli testnet-info`, `kcli faucet-info`, `kcli chain-info`, `kcli transfer --dry-run`, and live transfers against the monolith JSON-RPC have passed. `kcli transfer` and `kcli token-transfer` now also support `--no-wait` and explicit `--nonce <base64url>` so the live regression runner can create real non-waiting mempool pressure with deterministic nonce control.
- Live testnet producer assets and on-chain setup are ready: the latest public post-transfer balance check showed `357.12936826` KOIN, `2,849,756.12511139` VHP, and `256.70108774` Mana for `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`; `kcli get-producer-key 1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi` returns `AjyRoy9QlZP-AuojYV-cBlHC64mP-ZliaibjmjVnHL97`. The catch-up path now avoids scoring future gossip while the node is far behind and uses the local LIB as the sync anchor while live, matching legacy `koinos-p2p`. The prepared basedir now lives on the external SSD through a symlink at the original path after an internal-disk-space crash near height `4649031`. The monolith producer is now running on the live testnet with `features.block_producer: true`, P2P connected, gossip production gate open, heads matching the remote seed, accepted own PoB blocks observed through public RPC, and confirmed transactions submitted with `kcli` through the local monolith JSON-RPC. Keep it running in soak without re-registering the producer key unless explicitly required.
- The live producer transaction/operation/performance regression runner has started implementation. `scripts/live-producer-transaction-regression.sh` wraps `scripts/live-producer-transaction-regression.py`, writes JSON and Markdown result files, redacts obvious private material, and supports `--level 1` through `--level 5`. `kcli` supports `--password-file <path>` and `--yes` for unattended low-value mutating tests without logging wallet passwords; real transfers remain opt-in with `--submit-transfers`. First Level 1 run passed at `2026-05-30T15:38:46Z`; Level 2 real transfer smoke passed at `2026-05-30T16:04:50Z` with tx `0x12200870e83d9d6f65c9f8e09f9686c945bea1a751787097b53d0e105c2f9978d769` in public block `5383945`; the earlier Level 3 functional batch passed at `2026-05-31T06:31:56Z`; and bounded Level 5 sampling at `2026-05-31T06:34:52Z` passed all local phases but remains `blocked` only because Phase 7 needs a near-head external observer. The runner now implements true public-testnet mempool pressure: at `2026-05-31T09:07:17Z`, Level 3 submitted three signed `0.001` KOIN transfers through local monolith JSON-RPC with explicit nonces and `--no-wait`, observed `mempool.get_pending_transactions` counts `1`, `2`, and `3` after the submissions, verified the mempool drained to `0`, and found all three transactions in public block `5403845`. Phase 6 now supports real system-contract operations behind `--submit-system-ops`; after capping `kcli register-producer-key` RC limit to 10% of available mana and making the runner treat printed CLI errors as failures, a Level 3 run at `2026-05-31T09:18:55Z` passed real PoB/system operation validation: transfer smoke tx `0x1220133750d4b183a83914f95f77c71373458fcef80f896474f0bca621555c8a119a` in block `5404089`, non-waiting burst txs in block `5404093`, real PoB burn tx `0x1220f05585d81f302b9cfe600ea2acd14d7db63c49d3f4bb910687d557a5a91c97c9` in block `5404099`, and same-key producer registration tx `0x1220e0a72b37541a065a496fbc29a98a507b4770fb85e5401e1d51446b99ad3c4907` in block `5404105`. Re-registering the same producer key briefly triggered protocol-level `public key not yet active` warnings, then production resumed with own blocks at heights `5404127` and `5404129`; future runs require the separate `--submit-producer-registration` flag for real key registration, while `--submit-system-ops` alone performs the real burn only. The result was `warn`, not `fail`, because of known/transient warnings and zero score-threshold, checkpoint-mismatch, or gossip-gate rows.
- Sprint 5 performance work has started. `scripts/benchmark-monolith.py` is the new repeatable baseline harness for read-only JSON-RPC latency, live head progress, process RSS/CPU sampling, optional log-derived indexing speed, and opt-in startup measurement when launching a dedicated `koinos_node`. It writes `result.json` and `result.md` under `/private/tmp/knodel-monolith-benchmarks/<timestamp>` by default. The default attach mode does not restart the live testnet producer and does not mutate chain state. A short smoke against the live producer on 2026-06-01 passed with `chain.get_head_info` p50 `0.328 ms`, p95 `0.443 ms`, zero RPC errors, PID `32977`, RSS around `30.8 MB`, and CPU around `5.6%`; result files are under `/private/tmp/knodel-monolith-benchmarks/20260601T214350Z`.

## Important Commands

- Build monolith: `cmake --build vendor/koinos/koinos-node/build --target koinos_node --parallel`
- Gate D gossip smoke: `scripts/smoke-gossip-interop.sh`
- Gate E local smoke: `scripts/smoke-monolith-p2p-local.sh`
- Gate F short preflight: `SOAK_DURATION_SECONDS=20 SOAK_INTERVAL_SECONDS=5 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 scripts/soak-mainnet-p2p.sh`
- Gate F production soak: `SOAK_DURATION_SECONDS=172800 scripts/soak-mainnet-p2p.sh`
- Gate F corrected mainnet preflight: `SOAK_DURATION_SECONDS=360 SOAK_INTERVAL_SECONDS=15 SOAK_STARTUP_GRACE_SECONDS=300 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 SOAK_MIN_HEAD_HEIGHT=1000 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/0.0.0.0/tcp/8888 P2P_PEERS_FILE=scripts/mainnet-peer-candidates.txt scripts/soak-mainnet-p2p.sh`
- Gate F seed probe: `GOCACHE=/private/tmp/knodel-go-cache SEED_PROBE_ATTEMPTS=5 SEED_PROBE_TIMEOUT=8s SEED_PROBE_DELAY=10s SEED_PROBE_OUTPUT=scripts/mainnet-peer-validated.txt scripts/probe-mainnet-seeds.sh`
- Gate F fail-fast soak preflight: `SOAK_DURATION_SECONDS=180 SOAK_INTERVAL_SECONDS=10 SOAK_STARTUP_GRACE_SECONDS=120 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 scripts/soak-mainnet-p2p.sh`
- Gate F validated-peer soak: `SOAK_DURATION_SECONDS=300 SOAK_INTERVAL_SECONDS=15 SOAK_STARTUP_GRACE_SECONDS=240 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 SOAK_MIN_HEAD_HEIGHT=1000 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 P2P_PEERS_FILE=scripts/mainnet-peer-validated.txt scripts/soak-mainnet-p2p.sh`
- Gate F A/B peer acquisition harness: `AB_GO_DISCOVERY_ATTEMPTS=1 AB_GO_STABILITY_ATTEMPTS=3 scripts/ab-mainnet-peer-acquisition.sh`
- Sprint 2 producer build/test: `cmake --build vendor/koinos/koinos-node/build --target koinos_node koinos_block_producer_test --parallel && ctest --test-dir vendor/koinos/koinos-node/build --output-on-failure -R koinos_block_producer_test`
- Sprint 2 private federated smoke: `PRIVATE_TESTNET_BUILD=0 scripts/private-testnet-sprint2.sh`
- Sprint 2 private PoB genesis readiness probe: `scripts/probe-private-pob-genesis.js --require-ready /private/tmp/knodel-private-testnet/genesis_data.json`
- Sprint 2 private PoB smoke: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_BUILD=0 scripts/private-testnet-sprint2.sh`
- Sprint 2 private PoB 30-minute soak: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_BUILD=0 SOAK_DURATION_SECONDS=1800 SOAK_INTERVAL_SECONDS=60 scripts/private-testnet-sprint2.sh`
- Sprint 2 isolated seed-host external PoB soak: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_ROOT=/mnt/HC_Volume_105581636/knodel-external-signoff/knodel-private-testnet PRIVATE_TESTNET_BUILD=0 SOAK_DURATION_SECONDS=300 SOAK_INTERVAL_SECONDS=30 SEED_P2P_PORT=28888 PRODUCER_P2P_PORT=28889 OBSERVER_P2P_PORT=28890 SEED_RPC_PORT=28880 PRODUCER_RPC_PORT=28881 OBSERVER_RPC_PORT=28882 scripts/private-testnet-sprint2.sh`
- Sprint 2 shared/external PoB signoff: `PRODUCER_RPC_URL=http://producer:port/ OBSERVER_RPC_URL=http://observer:port/ EXTERNAL_TESTNET_DURATION_SECONDS=1800 scripts/external-pob-testnet-signoff.sh`
- Public testnet health: `curl -sS https://testnet.koinosfoundation.org/health`
- Public testnet head: `curl -sS https://testnet.koinosfoundation.org/jsonrpc -H 'content-type: application/json' --data '{"jsonrpc":"2.0","id":1,"method":"chain.get_head_info","params":{}}'`
- Public testnet P2P exposure check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/dns4/testnet.koinosfoundation.org/tcp/8888/p2p/QmYV414G6xRzkSUytntEsBsCSjXrVGubfYJn4vpeER2s2W' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=10s SEED_PROBE_PEER_RPC=0 scripts/probe-mainnet-seeds.sh`
- VPS1 legacy observer Peer RPC check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/ip4/46.225.170.6/tcp/28888/p2p/QmeJnbWzRZ91zgTDTxs1UdsbRFFM6B26PntLdk69N63ePY' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=10s scripts/probe-mainnet-seeds.sh`
- VM2 legacy observer Peer RPC check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/ip4/46.62.155.105/tcp/28888/p2p/QmXfSaJjSPSivJURC9RrCGKGmKtB3EA3AWEUksY2e189R3' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=12s scripts/probe-mainnet-seeds.sh`
- kcli testnet info: `kcli testnet-info`
- kcli configure testnet: `kcli config --network testnet --rpc https://testnet.koinosfoundation.org/jsonrpc`
- kcli config check: `kcli config --show`
- kcli chain check: `kcli chain-info`
- kcli wallet setup: `kcli generate-wallet` or `kcli import-wallet <WIF>`
- kcli faucet instructions: `kcli faucet-info`
- kcli balance: `kcli --network testnet balance`
- kcli transfer through local monolith JSON-RPC: `kcli -r http://127.0.0.1:18122 transfer <address> <amount>`; use `--dry-run` first.
- Live producer regression Level 1: `scripts/live-producer-transaction-regression.sh --level 1 --sample-seconds 0`
- Sprint 5 read-only live producer benchmark: `scripts/benchmark-monolith.py --rpc-url http://127.0.0.1:18122/ --pid-file /Users/pgarcgo/.kcli/knodel-testnet-producer/live-testnet-sync.pid --duration-seconds 60 --interval-seconds 1 --latency-requests 200`
- Sprint 5 dedicated startup benchmark: `scripts/benchmark-monolith.py --launch-bin vendor/koinos/koinos-node/build/koinos_node --launch-basedir <basedir> --jsonrpc-listen 127.0.0.1:<port> --rpc-url http://127.0.0.1:<port>/ --disable p2p --duration-seconds 10`
- Full live producer performance regression: `RECIPIENT_ADDRESS=<controlled-address> OBSERVER_RPC=<caught-up-observer-rpc> KCLI_PASSWORD_FILE=<0600-password-file> TRANSFER_AMOUNT=0.001 TX_COUNT=5 scripts/live-producer-transaction-regression.sh --level 5 --submit-transfers --sample-seconds 300 --sample-interval 15`
- Live producer transfer smoke dry-run: `RECIPIENT_ADDRESS=<controlled-address> KCLI_PASSWORD_FILE=<0600-password-file> scripts/live-producer-transaction-regression.sh --level 2`
- Live producer transfer smoke with interactive wallet unlock: `RECIPIENT_ADDRESS=<controlled-address> TRANSFER_AMOUNT=0.001 scripts/live-producer-transaction-regression.sh --level 2 --interactive-kcli`
- Live producer transfer smoke with real low-value transfer: `RECIPIENT_ADDRESS=<controlled-address> KCLI_PASSWORD_FILE=<0600-password-file> TRANSFER_AMOUNT=0.001 scripts/live-producer-transaction-regression.sh --level 2 --submit-transfers`
- kcli VHP burn dry-run: `kcli --network testnet burn --dry-run --amount <KOIN_AMOUNT>`
- kcli producer key registration dry-run: `kcli --network testnet register-producer-key --dry-run <producerAddressOrPublicKey> [publicKey]`
- Live testnet prepared producer faucet command: `/faucet 1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`
- Live testnet prepared producer key registration dry-run: `kcli register-producer-key --dry-run AjyRoy9QlZP-AuojYV-cBlHC64mP-ZliaibjmjVnHL97`
- Gate F 5h soak request attempted before seed fix: `SOAK_DURATION_SECONDS=18000 SOAK_INTERVAL_SECONDS=60 JSONRPC_PORT=18082 scripts/soak-mainnet-p2p.sh`; aborted because it produced no real peers.
- Sprint 1.1 backup restore dry-run: `scripts/verify-monolith-backup-restore.sh --dry-run --workdir /Volumes/external/knodel-monolith-restore`
- Sprint 1.1 full backup restore: `scripts/verify-monolith-backup-restore.sh --download --workdir /Volumes/external/knodel-monolith-restore`
- Sprint 4 migration dry-run: `vendor/koinos/koinos-node/tools/migrate_block_store.sh --dry-run --basedir /Volumes/external/knodel-monolith-restore/basedir`
- Sprint 4 migration run with sampled SHA-256 verification: `vendor/koinos/koinos-node/tools/migrate_block_store.sh --basedir /path/to/legacy-basedir --force --verify sample`
- Sprint 4 migration run with full SHA-256 verification: `vendor/koinos/koinos-node/tools/migrate_block_store.sh --basedir /path/to/legacy-basedir --force --verify full`
- Sprint 4 migrated-basedir node smoke: `vendor/koinos/koinos-node/tools/migrate_block_store.sh --verify-node-only --basedir /Volumes/external/knodel-monolith-restore/basedir --jsonrpc-port 18113`
- Sprint 4.3 gRPC monolith smoke: `scripts/compare-grpc-parity.sh 127.0.0.1:<monolith-grpc-port>`
- Sprint 4.3 gRPC legacy parity: `GRPC_PARITY_FAIL_ON_MISMATCH=1 scripts/compare-grpc-parity.sh 127.0.0.1:<monolith-grpc-port> 127.0.0.1:<legacy-grpc-port>`
- Sprint 4.3 gRPC sequential file parity: `GRPC_PARITY_LEGACY_INPUT=/path/to/legacy.json GRPC_PARITY_MONOLITH_INPUT=/path/to/monolith.json scripts/compare-grpc-parity.sh`
- Sprint 4.3 gRPC client validation: `MONOLITH_GRPC_TARGET=127.0.0.1:<grpc-port> MONOLITH_JSONRPC_URL=http://127.0.0.1:<jsonrpc-port> scripts/validate-grpc-client-compatibility.sh`
- Sprint 4.4 fetch pinned upstream integration tests: `scripts/fetch-koinos-integration-tests.sh`
- Sprint 4.4 inventory upstream tests: `scripts/run-koinos-integration-compat.sh inventory`
- Sprint 4.4 Docker legacy baseline for a selected test: `scripts/run-koinos-integration-compat.sh legacy <test-name>`
- Sprint 4.4 native legacy baseline without Docker: `scripts/run-koinos-integration-compat.sh legacy-native <test-name>`; selected tests are `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `transaction_error`, `koin`, `vhp`, `pob`, and `propose_block`.
- Sprint 4.4 monolith compatibility run: `scripts/run-koinos-integration-compat.sh monolith <test-name>`; selected tests are `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `koin`, `vhp`, and `pob`.
- Sprint 1.2 capture legacy baseline: `python3 scripts/compare-jsonrpc-parity.py write-baseline --url http://127.0.0.1:18084/ --flavor legacy --output /private/tmp/knodel-jsonrpc-legacy-baseline.json`
- Sprint 1.2 compare monolith: `python3 scripts/compare-jsonrpc-parity.py compare-baseline --baseline /private/tmp/knodel-jsonrpc-legacy-baseline.json --url http://127.0.0.1:18083/ --flavor monolith --output /private/tmp/knodel-jsonrpc-parity-comparison.json`
- Sprint 1.3 mempool build/test: `cmake --build vendor/koinos/koinos-node/build --target koinos_node koinos_mempool_adapter_test koinos_block_producer_test --parallel && ctest --test-dir vendor/koinos/koinos-node/build --output-on-failure -R 'koinos_(mempool_adapter|block_producer|gorpc_codec|p2p_one_peer_sync)_test'`
- Frontend/Electron build: `npm run build`

## Next Critical Path

Immediate critical path is now split between implementation progress, mainnet networking diagnostics, and live external testnet validation. Sprint 1.3 is complete locally, and Sprint 2 Phase A private federated validation, Phase B private PoB validation, the 30-minute private PoB soak, isolated seed-host external validation, local `kcli` testnet configuration, the live testnet non-producing observer sync, local live-producer hot-key/basedir preparation, VHP burn, producer-key registration, full live-head catch-up, controlled producer-mode startup, accepted live-testnet PoB block production, a confirmed `kcli transfer` through the local monolith JSON-RPC, Level 3 live transaction regression, and a 48h live-producer soak milestone are complete. Gate F mainnet networking signoff still blocks a mainnet canary, but it does not block continued monolith implementation. The A/B peer acquisition harness is available for future mainnet peer-window classification. Sprint 2.4 should continue as a longer live-producer soak plus Level 4/5 completion once an external legacy observer reaches near-head; current VPS observers are still far behind and cannot yet act as independent witnesses. Sprint 4.3 gRPC compatibility and Sprint 4.4 legacy integration-test compatibility are complete for their selected release-gate suites. Sprint 5 is now active: the first step is to capture repeatable monolith and legacy performance baselines with `scripts/benchmark-monolith.py`, then use those baselines to drive RocksDB tuning, thread-pool sizing, shutdown/error hardening, and metrics/logging improvements.
