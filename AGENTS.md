# Teleno Codex Project Memory

Last updated: 2026-06-11

This file is the local project memory for Codex work in this repo. Codex Desktop does not guarantee that arbitrary repo files are always read automatically in every thread, so explicitly ask to read `AGENTS.md` when resuming if the context was compacted or lost.

This repository is the independent `teleno` codebase. It is copied from Knodel, but it owns the monolithic Koinos node app. Knodel remains the microservice app in its own repository.

## Active Project Boundary

- Active monolith repository: `/Users/pgarcgo/code/teleno`
- Active remote: `https://github.com/pgarciagon/teleno.git`
- Active branch for the current release track: `main`
- `Teleno` owns the independent monolithic Koinos node app, including monolith build scripts, monolith runtime fixes, mainnet/testnet canary reports, monolith roadmap docs, and monolith release packaging.
- `Knodel` owns the separate microservice app and must not receive monolith-track work unless the user explicitly asks for a Knodel/microservice change.
- If a resumed Codex session starts in `/Users/pgarcgo/code/knodel`, switch to `/Users/pgarcgo/code/teleno` before editing, committing, pushing, building, or launching monolith work.
- Historical notes below may still mention Knodel paths or result directories because this project was split from Knodel. Treat those as inherited historical evidence unless the note explicitly refers to the separate microservice app.

## Latest Repository Correction

- On 2026-06-11, active legacy cleanup began in `teleno`: old Knodel/microservice operator docs and native microservice build/start/package scripts were removed from the active command surface. Retain legacy-facing material only when it proves protocol parity, client compatibility, migration/restore safety, or release-gate validation. The retained evidence map is `docs/compatibility/README.md`.
- On 2026-06-11, legacy service submodules were removed from the active tree. The only retained legacy compatibility submodule is Go `koinos-p2p`, moved to `compat/legacy-services/koinos-p2p` for seed/gossip/Peer RPC probes. `vendor/koinos/koinos` remains for active genesis/config resources, and `node/teleno-node` is now the current monolith source path. Native legacy baseline scripts now require external binary paths instead of bundled service submodules.
- On 2026-06-11, the active local working directory was corrected to `/Users/pgarcgo/code/teleno`. If a future Codex thread starts with `/Users/pgarcgo/code/koinosgui`, treat that as stale thread context and switch to `/Users/pgarcgo/code/teleno` before reading, editing, building, packaging, launching, or committing. The README now displays the project logo from `assets/newbranding/logo.png` above the `Teleno` heading.
- On 2026-06-04, the monolith mainnet canary build fix was first committed and pushed by mistake to `pgarciagon/knodel` as commit `2abc62c`. The same work was then ported to the correct repository, `pgarciagon/koinosgui`, committed on `main` as `5a09f85` (`Fix monolith mainnet canary build`), and pushed to `origin/main`.
- The correct koinosGUI commit includes `config/mainnet-observer.yml`, `docs/roadmap/monolith/networking/MONOLITH_MAINNET_CANARY_REPORT.md`, Sprint 6.1 plan updates, mainnet peer updates, the remote OpenSSL/yaml-cpp build fix, and the monolith YAML config parser fix.
- The accidental Knodel commit should be reverted with a normal `git revert 2abc62c` only if the user asks to clean up Knodel. Do not force-push or rewrite Knodel history without explicit instruction.

## Project Mission

Act as an expert blockchain engineer for this project. The goal is to build an optimized monolithic Koinos block-producing node app that is easy for end users to launch and operate. The current primary target is macOS; Windows optimization will follow. All implementation decisions must preserve compatibility with the Koinos protocol and avoid shortcuts that would diverge from mainnet behavior.

All project documentation must be written in English, even when discussion with the user happens in Spanish.

## Mainnet Safety Guardrail

- Address `14MHW6TF8gw8EuMRLCJc2PQHLzZLKuwGqb` is a real funded mainnet producer address. Do not perform any mutating mainnet operation involving this address, including transfers, burns, producer-key registrations, producer setup changes, default-account changes, config writes that would target it, or any transaction signing/submission. Treat it as read-only only, and only inspect it if the user explicitly asks. Before any future `kcli`, GUI, or node operation that can mutate chain or producer state, verify the selected network is not mainnet and verify the target/signer/producer address is not `14MHW6TF8gw8EuMRLCJc2PQHLzZLKuwGqb`.

## Merkle Mismatch Recovery Guardrail

- If the monolith reports `block previous state merkle mismatch` or another persistent state merkle mismatch, do not try to recover by clearing `chain/blockchain`, starting from an empty state DB, or forcing a fresh full resync as the first action. Preserve the existing state DB, keep block production disabled, set `chain.verify-blocks: true`, and restart the node as an observer so full block validation can repair/advance the state from the restored database. Only consider moving/deleting the state DB or doing a clean-state rebuild after explicit user approval and clear evidence that validation-based recovery failed. On 2026-06-09 this validation-first recovery worked on the restored mainnet basedir: enabling `verify-blocks: true` indexed the missing 60 blocks and resumed live block application with five peers.

## Current Monolith Status

- Detailed current monolith status lives in `docs/roadmap/monolith/core/CURRENT_MONOLITH_STATUS.md`. Read that document when you need the current status, validation history, report references, or live testnet/mainnet operational context.

## What Works Now

- `node/teleno-node/build/koinos_node` builds with `cpp-libp2p`.
- JSON-RPC responds from the monolith with local config data and is now compatible with `kcli`/koilib public JSON conventions for the live transfer path, including Base58/hex request normalization, websafe byte responses, and HTTP keep-alive reuse.
- Peer RPC compatibility is covered by Go-generated fixtures and live Go peer interop.
- One-peer sync is covered by controlled C++/Go live tests and applies blocks by RPC.
- GossipSub interop is covered both directions for blocks and transactions against a controlled Go peer.
- Teleno starts one monolith node process, derives component health from monolith logs, filters component logs, and supports presets/restart without exposing the old microservice command surface.
- P2P peer snapshots are logged by the monolith as `[p2p] Connected peers:` rows so the Teleno dashboard parser can consume them.
- Real seed backup restore works through the monolith layout: converted import result was `records=40278912 blocks=40278911 meta=1 bytes=83883179053`; `chain.get_head_info` returned height `36180898` from the restored data.
- JSON-RPC parity harness covers 21 methods across chain, block_store, mempool, contract_meta_store, transaction_store, and account_history. It captures legacy baseline from `http://127.0.0.1:18084/` and compares monolith `http://127.0.0.1:18083/`.
- The `koinos/koinos-integration-tests` compatibility runner is in place. Native legacy mode passes the selected high-value single-node baseline without Docker, and monolith mode passes the selected public JSON-RPC suite: `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `koin`, `vhp`, and `pob`. JSON-RPC output rewrites protobuf bytes using `koinos.btype`, and JSON-RPC input normalizes full block payloads for `submit_block`/`propose_block`.
- gRPC compatibility now uses the public typed protobuf service `koinos.services.koinos`. `GRPCServer` routes generated service methods directly to monolith component APIs, exposes `get_gossip_status`, maps validation errors to `INVALID_ARGUMENT`, and `scripts/compare-grpc-parity.sh` can run monolith-only smoke probes, legacy-vs-monolith endpoint comparisons, or sequential saved-file comparisons through generated protobuf stubs.
- Mempool end-to-end wiring is in place for the monolith. Accepted transactions are added to the real mempool, `mempool.get_pending_transactions` returns them, pending RC reservations affect `check_pending_account_resources`, nonce duplication/next-nonce checks work through the same in-process RPC path used by chain, and `mempool.transaction-expiration` drives a 1s prune loop.
- Mainnet P2P no longer has a pure local timing blocker: seed dialing is paced like legacy, duplicate dials are suppressed, and the monolith can answer remote Peer RPC. The latest validation proves local build/tests pass and Go itself cannot currently find a Peer RPC-capable public target from this host. `scripts/probe-mainnet-seeds.sh` now treats raw libp2p dial success as insufficient by default: `OK` requires Koinos Peer RPC `GetChainID` and `GetHeadBlock`.
- Block producer backend now submits real populated blocks instead of empty proposals. `koinos_block_producer_test` verifies local federated block assembly, resource-bounded transaction inclusion, accepted receipt handling, and chain-reported failed transaction prune/retry behavior.
- Private Sprint 2 federated and PoB smokes work with three local monolith nodes. The harness patches a temporary Harbinger genesis for federated mode and generates a deterministic system-contract PoB genesis for PoB mode, forces gossip for private bootstrap, starts seed/producer/observer, connects the observer directly to both seed and producer, verifies observer head/block-store height, checks transaction/contract-meta service health, and supports duration-based private PoB soaks through `SOAK_DURATION_SECONDS`.
- Public testnet JSON-RPC and P2P are reachable. A non-producing monolith observer against `testnet.koinosfoundation.org` completed the first live observer sync check, and the `kcli` producer-control wallet setup, VHP burn, producer-key registration, full catch-up to the live head, controlled switch to `features.block_producer: true`, accepted live PoB block production, and a real `kcli transfer` through the monolith JSON-RPC are complete. Public RPC verified produced blocks at heights `5378368`, `5378369`, `5378792`, and transaction block `5380195` signed/produced by the configured producer address. The live sync path now follows legacy behavior by anchoring peer branch checks on the local LIB rather than the volatile local head, so normal competing live forks above LIB do not cause seed score-threshold disconnects. Use `SEED_PROBE_PEER_RPC=0` for a raw public P2P exposure check; the lightweight Go probe is not a full Koinos peer and repeated Peer RPC probes can temporarily trigger the Go p2p source-IP error scorer on the single-node testnet.
- VPS1 legacy observer is live and externally reachable at `/ip4/46.225.170.6/tcp/28888/p2p/QmeJnbWzRZ91zgTDTxs1UdsbRFFM6B26PntLdk69N63ePY`. It has correct chain ID and Peer RPC, connects to `testnet.koinosfoundation.org`, and is syncing from genesis through the legacy stack. Treat it as a topology/stability peer now and as an independent observer only after catch-up completes.
- VM2 legacy observer is live at `/ip4/46.62.155.105/tcp/28888/p2p/QmXfSaJjSPSivJURC9RrCGKGmKtB3EA3AWEUksY2e189R3` and syncing from genesis. Treat it as a syncing external observer only; full Peer RPC validation is still pending because `GetHeadBlock` reset during initial catch-up load.
- `kcli` testnet client setup is complete: current local version `1.4.0`, default network `testnet`, RPC `https://testnet.koinosfoundation.org/jsonrpc`; `kcli testnet-info`, `kcli faucet-info`, `kcli chain-info`, `kcli transfer --dry-run`, and live transfers against the monolith JSON-RPC have passed. `kcli transfer` and `kcli token-transfer` now also support `--no-wait` and explicit `--nonce <base64url>` so the live regression runner can create real non-waiting mempool pressure with deterministic nonce control.
- Live testnet producer assets and on-chain setup are ready: the latest public post-transfer balance check showed `357.12936826` KOIN, `2,849,756.12511139` VHP, and `256.70108774` Mana for `1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`; `kcli get-producer-key 1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi` returns `AjyRoy9QlZP-AuojYV-cBlHC64mP-ZliaibjmjVnHL97`. The catch-up path now avoids scoring future gossip while the node is far behind and uses the local LIB as the sync anchor while live, matching legacy `koinos-p2p`. The prepared basedir now lives on the external SSD through a symlink at the original path after an internal-disk-space crash near height `4649031`. The monolith producer is now running on the live testnet with `features.block_producer: true`, P2P connected, gossip production gate open, heads matching the remote seed, accepted own PoB blocks observed through public RPC, and confirmed transactions submitted with `kcli` through the local monolith JSON-RPC. Keep it running in soak without re-registering the producer key unless explicitly required.
- The live producer transaction/operation/performance regression runner has started implementation. `scripts/live-producer-transaction-regression.sh` wraps `scripts/live-producer-transaction-regression.py`, writes JSON and Markdown result files, redacts obvious private material, and supports `--level 1` through `--level 5`. `kcli` supports `--password-file <path>` and `--yes` for unattended low-value mutating tests without logging wallet passwords; real transfers remain opt-in with `--submit-transfers`. First Level 1 run passed at `2026-05-30T15:38:46Z`; Level 2 real transfer smoke passed at `2026-05-30T16:04:50Z` with tx `0x12200870e83d9d6f65c9f8e09f9686c945bea1a751787097b53d0e105c2f9978d769` in public block `5383945`; the earlier Level 3 functional batch passed at `2026-05-31T06:31:56Z`; and bounded Level 5 sampling at `2026-05-31T06:34:52Z` passed all local phases but was blocked because Phase 7 needed a near-head external observer. The runner now implements true public-testnet mempool pressure: at `2026-05-31T09:07:17Z`, Level 3 submitted three signed `0.001` KOIN transfers through local monolith JSON-RPC with explicit nonces and `--no-wait`, observed `mempool.get_pending_transactions` counts `1`, `2`, and `3` after the submissions, verified the mempool drained to `0`, and found all three transactions in public block `5403845`. Phase 6 now supports real system-contract operations behind `--submit-system-ops`; after capping `kcli register-producer-key` RC limit to 10% of available mana and making the runner treat printed CLI errors as failures, a Level 3 run at `2026-05-31T09:18:55Z` passed real PoB/system operation validation: transfer smoke tx `0x1220133750d4b183a83914f95f77c71373458fcef80f896474f0bca621555c8a119a` in block `5404089`, non-waiting burst txs in block `5404093`, real PoB burn tx `0x1220f05585d81f302b9cfe600ea2acd14d7db63c49d3f4bb910687d557a5a91c97c9` in block `5404099`, and same-key producer registration tx `0x1220e0a72b37541a065a496fbc29a98a507b4770fb85e5401e1d51446b99ad3c4907` in block `5404105`. Re-registering the same producer key briefly triggered protocol-level `public key not yet active` warnings, then production resumed with own blocks at heights `5404127` and `5404129`; future runs require the separate `--submit-producer-registration` flag for real key registration, while `--submit-system-ops` alone performs the real burn only. On 2026-06-02, full Level 5 passed with VPS1 as the independent legacy observer witness and `TX_INTERVAL=0`: transfer smoke tx `0x12208e5456da712437310fb617915f588a27576b6702acfa0023ea3a006d11d512f1` confirmed in locally produced public block `5468691`; five explicit-nonce burst transfers all appeared in public block `5468695` after local mempool counts rose from `0` to `5` and drained; Phase 7 observer lag was `0`; Phase 9 collected 20 samples with local RPC p95 `3.45ms` and max `4.67ms`. Result files are under `/private/tmp/knodel-transaction-regression/20260602T163636Z`.
- Sprint 5 performance work has started. `scripts/benchmark-monolith.py` is the repeatable monolith baseline harness for read-only JSON-RPC latency, live head progress, process RSS/CPU sampling, optional log-derived indexing speed, and opt-in startup measurement when launching a dedicated `koinos_node`; it writes `result.json` and `result.md` under `/private/tmp/knodel-monolith-benchmarks/<timestamp>` by default. `scripts/benchmark-legacy-native-jsonrpc.sh` is the same-machine local legacy wrapper: it starts an isolated native GarageMQ + chain + block_store + mempool + JSON-RPC stack on ports `25674`/`35674`/`28081`, benchmarks it, writes service-level stack RSS/CPU samples, and cleans up before exit. `scripts/benchmark-transaction-submission.js` is the live monolith submit benchmark: it builds valid koilib KOIN transfers, decrypts the local kcli wallet only in memory through the existing `0600` password-file path, times operation build, prepare, sign, and direct `chain.submit_transaction`, samples mempool growth/drain, and verifies public inclusion without printing or writing private material. `scripts/benchmark-monolith-startup.sh` runs repeated temporary monolith launches with P2P/gRPC/block_producer disabled and records spawn-to-JSON-RPC readiness. `scripts/benchmark-monolith-sync.py` launches an isolated non-producing live-testnet observer, samples chain/block-store/remote-head progress plus process usage and P2P log counters, then terminates the observer. `scripts/benchmark-monolith-index-replay.py` prepares bounded block-store subsets from the restored external-SSD basedir and measures chain replay/indexing throughput from the monolith indexer completion log. A 5-minute live monolith baseline on 2026-06-02 passed with `chain.get_head_info` p95 `0.575 ms`, RSS p95 `44.299 MB`, and CPU p95 `11.93%`. The same-machine native legacy baseline passed with p95 `1.43 ms` and minimal-stack RSS mean `45.413 MB`. The first live transaction-submit baseline passed with five `0.001` KOIN transfers included in public block `5470571`; direct submit p95 was `12.155 ms`, full prepare/sign/submit p95 was `17.173 ms`, mempool count rose from `0` to `5` then drained to `0`, and the producer log recorded the same block with `5 transactions`. The monolith startup baseline passed five iterations on ports `28100-28104` with startup-to-JSON-RPC min `229.397 ms`, mean `263.672 ms`, p50 `232.715 ms`, p95 `348.374 ms`, and max `372.723 ms`, with no warning/error log matches and no leftover startup processes. The live P2P sync baseline passed a 120s isolated observer run on port `28200`: startup `181.387 ms`, local head `0 -> 9300`, average `77.494` blocks/sec, window p95 `88.221` blocks/sec, one handshake, one connected peer, `21` sync rows, zero warnings, zero score-threshold/checkpoint/block-application failures, RSS p95 `49.605 MB`, and no leftover observer process. Result files are under `/private/tmp/knodel-monolith-benchmarks/sprint5-live-monolith-20260602`, `/private/tmp/knodel-legacy-native-benchmark/20260602T175845Z`, `/private/tmp/knodel-transaction-benchmarks/sprint5-live-submit-20260602`, `/private/tmp/knodel-monolith-startup-benchmark/sprint5-startup-20260602`, and `/private/tmp/knodel-monolith-sync-benchmark/sprint5-testnet-sync-20260602`. Sprint 5.1 live baselines are captured; submit latency is above the aspirational `<2 ms` target and should feed later tuning, while the old `10,000-15,000` blocks/sec indexing target is now validated as an offline/replay metric rather than a live P2P sync metric.
- Sprint 5.2 RocksDB tuning is implemented in code. `NodeConfig` accepts a `rocksdb:` section for `block-cache-mb`, `max-background-jobs`, `bytes-per-sync`, `default-block-size`, `blocks-block-size`, `target-file-size-base`, `max-bytes-for-level-base`, `write-buffer-size`, `db-write-buffer-size`, `max-write-buffer-number`, and `blocks-compression`. The monolith now uses one shared LRU block cache across all six column families, `4KB` table blocks plus Bloom filters for chain/default and metadata/index point-lookups, `64KB` table blocks plus Bloom filters for the `blocks` column family, prefix Bloom filtering for account history, configurable background compaction/flush jobs, shared DB write-buffer limits, and pipelined writes. Compression defaults to requested `zstd`, but the node selects the best codec supported by the linked RocksDB library at startup; the current Hunter RocksDB build has ZSTD/Snappy disabled, so the isolated smoke selected `none` and logged that as info, not a warning. Verification passed with `cmake --build node/teleno-node/build --target koinos_node --parallel` and a one-iteration isolated startup smoke on port `28210`; evidence is under `/private/tmp/knodel-rocksdb-tuning-smoke-20260602`. The live producer was not restarted and remained healthy on `127.0.0.1:18122`.
- Sprint 5.3 thread-pool tuning is implemented for the current monolith runtime. Startup logs now report the effective thread topology: `main_ioc`, chain jobs, JSON-RPC session limit, gRPC pollers, requested P2P IO threads, effective P2P IO threads, per-peer P2P sync threads, and RocksDB background jobs. cpp-libp2p intentionally remains on one effective IO runner because the current transport documents that connection/muxer state is unsafe across multiple runners; if `p2p.jobs` requests more, the transport logs the requested count and the effective serialized count. JSON-RPC now enforces `jsonrpc.jobs` as a hard active-session limit instead of creating unbounded detached session threads, returning `503` with a JSON-RPC error if saturated. `scripts/benchmark-monolith-sync.py` now writes configurable `chain.jobs`, `jsonrpc.jobs`, `grpc.jobs`, `p2p.jobs`, and RocksDB job settings into each temporary observer config, records runtime topology/RocksDB tuning from logs, and `scripts/benchmark-monolith-thread-matrix.py` runs repeatable isolated live-sync matrices. Verification passed: Python syntax checks, `cmake --build node/teleno-node/build --target koinos_node --parallel`, and a three-row 20s live-testnet matrix under `/private/tmp/knodel-monolith-thread-matrix/sprint5-thread-matrix-20260602`. Results: baseline `78.38` blocks/sec, bounded `78.165` blocks/sec, db-heavy `76.281` blocks/sec; all rows had zero warnings, score-threshold rows, checkpoint mismatches, or block-application failures. The recommended live-sync profile for now is bounded/conservative: `jsonrpc.jobs: 2`, `p2p.jobs: 1`, `grpc.jobs: 1` when gRPC is disabled, and `rocksdb.max-background-jobs: 2-4`. The mixed RPC/submit/sync stress gate has now passed as the deadlock/load follow-up.
- Sprint 5.4 error-handling hardening has a first implementation slice in place. `ServiceRegistry` now tracks started components, logs start failures, stops already-started components if a later component fails during startup, skips never-started components during shutdown, and continues stopping remaining components when a standard or unknown stop exception is thrown. JSON-RPC sessions and dispatch now log/return explicit failures for unknown exceptions, incoming Peer RPC maps unknown handler exceptions to a server-error response, the periodic metrics tick logs collection failures, and `main()` reports unknown top-level exceptions. Broad catches were reviewed: config fallback catches remain intentionally quiet before logging is initialized, and the async monolith RPC client preserves propagation through promises/futures. `koinos_service_registry_test` covers standard and unknown startup/stop failures and passed. A short live-testnet sync/SIGTERM smoke on port `28400` also passed under `/private/tmp/knodel-error-hardening-sync-20260602`: local head `0 -> 1555`, `77.719` blocks/sec, one handshake, two connected-peer snapshots, zero warning/error/score-threshold/checkpoint/block-application rows, and no leftover observer process. The live producer was not restarted and stayed healthy on `127.0.0.1:18122` at public-testnet head `5473896`. The mixed stress wrapper now adds concurrent RPC/submit/sync evidence without restarting the live producer.
- Sprint 5.5 logging hardening has a first implementation slice in place. Monolith lifecycle rows now use `[node]`, RocksDB open failure uses `[db]`, and the compact `[metrics]` row emits immediately at readiness and then every `60s` with `head_height`, `lib`, `blocks_per_sec`, `pending_txs`, `peer_count`, `rss_bytes`, `rss_mb`, and `components`. RSS collection uses macOS `task_info`, Linux `/proc/self/statm`, and `0` fallback elsewhere. Knodel's 512KB native-service log buffer is covered by `electron/lib/logs-service.test.ts`, which appends more than `512KB` and verifies the compact metrics row remains parseable after trimming. Verification passed with `cmake --build node/teleno-node/build --target koinos_node --parallel`, `npx vitest run electron/lib/logs-service.test.ts`, and a one-iteration isolated startup smoke under `/private/tmp/knodel-logging-smoke-20260602`; the smoke emitted `[metrics] head_height=0 lib=0 blocks_per_sec=0.000 pending_txs=0 peer_count=0 rss_bytes=24379392 rss_mb=23.250 components=3` and no leftover process. Some deeper legacy chain/mempool internals still emit old unprefixed rows, so prefix normalization is not fully complete below the monolith wrapper. The live producer was not restarted and stayed healthy on `127.0.0.1:18122` at public-testnet head `5474776`.
- Sprint 5 mixed RPC/submit/sync stress is now captured as a reusable gate. `scripts/benchmark-monolith-mixed-stress.py` composes the isolated live observer sync benchmark, live producer read-only RPC/process benchmark, and koilib transaction submission benchmark into one pass/warn/fail summary while keeping real transfers opt-in. A 30s dry run passed sync/RPC and warned only because submit was dry-run. The 120s live-submit pass on 2026-06-03 passed at commit `da2f21c`: the temporary observer synced `0 -> 9300` at `77.494` blocks/sec with one handshake, one peer, `21` sync rows, and zero warning/score-threshold/checkpoint/block-application rows; live producer RPC p95s were `0.685 ms` for `chain.get_head_info`, `0.342 ms` for `block_store.get_highest_block`, and `1.436 ms` for `mempool.get_pending_transactions`; three `0.001` KOIN transfers submitted through the local monolith JSON-RPC were all included in public block `5479327`, direct submit p95 was `10.806 ms`, and no child check failed. Result files are under `/private/tmp/knodel-monolith-mixed-stress/live-submit-20260603`. The live producer was left running and healthy at local head `5479367` after the run.
- Sprint 5 offline/replay indexing is now captured as a reusable gate. `scripts/benchmark-monolith-index-replay.py` starts the restored block store with `chain` disabled so the source basedir is not indexed/mutated, copies a bounded block-only subset through JSON-RPC `block_store.get_blocks_by_height` -> `block_store.add_block`, restarts the target node, and parses `Finished indexing ... blocks` from the chain indexer log. `jsonrpc_server.cpp` now normalizes public Koinos JSON byte/address encodings for `block_store.add_block`, allowing blocks returned by block-store read APIs to round-trip back through JSON-RPC. Receipts are optional via `--include-receipts`; the default omits them because chain replay/indexing only requires blocks and receipts can produce unnecessarily large JSON-RPC payloads. The 100-block smoke passed at `19,849.146` blocks/sec with zero warning/error rows. The 10,000-block sample passed on 2026-06-03 at commit `da2f21c`: copied `10,000` blocks in `15.547s`, indexed `10,000` blocks in `0.59243s` (`16,879.631` blocks/sec), final head `10000`, zero warning/error rows, and report files under `/Volumes/external/knodel-monolith-index-replay-benchmark/sample-10000-20260603`.
- Sprint 6.3 packaging readiness has a first release-gate slice in place. The active Teleno package scripts use `build:icon:mac`, build the Electron app, run `stage`, verify `build/bundle-staging/teleno`, run `electron-builder`, and then verify the packaged app. `scripts/verify-package-staging.js` now checks the staged Teleno bundle before packaging, including `teleno_node`, config templates, and core contract ABIs. `scripts/verify-packaged-app.js` checks the final packaged app, including `app.asar`, `Resources/teleno/bin/teleno_node`, and config templates. Historical `Knodel.app` evidence from 2026-06-03 remains useful only as old native-bundle completeness evidence, not as final Teleno release evidence. Signed/notarized DMG, end-user migration docs, and the final release-candidate checklist remain pending.

## Important Commands

- Build monolith: `./scripts/build-cpp-libp2p-koinos.sh`
- Gate D gossip smoke: `scripts/smoke-gossip-interop.sh`
- Gate E local smoke: `scripts/smoke-monolith-p2p-local.sh`
- Gate F short preflight: `SOAK_DURATION_SECONDS=20 SOAK_INTERVAL_SECONDS=5 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 scripts/soak-mainnet-p2p.sh`
- Gate F production soak: `SOAK_DURATION_SECONDS=172800 scripts/soak-mainnet-p2p.sh`
- Gate F corrected mainnet preflight: `SOAK_DURATION_SECONDS=360 SOAK_INTERVAL_SECONDS=15 SOAK_STARTUP_GRACE_SECONDS=300 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 SOAK_MIN_HEAD_HEIGHT=1000 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/0.0.0.0/tcp/8888 P2P_PEERS_FILE=scripts/mainnet-peer-candidates.txt scripts/soak-mainnet-p2p.sh`
- Gate F seed probe: `GOCACHE=/private/tmp/knodel-go-cache SEED_PROBE_ATTEMPTS=5 SEED_PROBE_TIMEOUT=8s SEED_PROBE_DELAY=10s SEED_PROBE_OUTPUT=scripts/mainnet-peer-validated.txt scripts/probe-mainnet-seeds.sh`
- Gate F fail-fast soak preflight: `SOAK_DURATION_SECONDS=180 SOAK_INTERVAL_SECONDS=10 SOAK_STARTUP_GRACE_SECONDS=120 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 scripts/soak-mainnet-p2p.sh`
- Gate F validated-peer soak: `SOAK_DURATION_SECONDS=300 SOAK_INTERVAL_SECONDS=15 SOAK_STARTUP_GRACE_SECONDS=240 SOAK_REQUIRE_PROGRESS=1 SOAK_REQUIRE_HEAD_PROGRESS=1 SOAK_MIN_HEAD_HEIGHT=1000 JSONRPC_PORT=18082 P2P_LISTEN=/ip4/127.0.0.1/tcp/0 P2P_PEERS_FILE=scripts/mainnet-peer-validated.txt scripts/soak-mainnet-p2p.sh`
- Gate F A/B peer acquisition harness: `AB_GO_DISCOVERY_ATTEMPTS=1 AB_GO_STABILITY_ATTEMPTS=3 scripts/ab-mainnet-peer-acquisition.sh`
- Sprint 2 producer build/test: `cmake --build node/teleno-node/build --target koinos_node koinos_block_producer_test --parallel && ctest --test-dir node/teleno-node/build --output-on-failure -R koinos_block_producer_test`
- Sprint 2 private federated smoke: `PRIVATE_TESTNET_BUILD=0 scripts/private-testnet-sprint2.sh`
- Sprint 2 private PoB genesis readiness probe: `scripts/probe-private-pob-genesis.js --require-ready /private/tmp/knodel-private-testnet/genesis_data.json`
- Sprint 2 private PoB smoke: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_BUILD=0 scripts/private-testnet-sprint2.sh`
- Sprint 2 private PoB 30-minute soak: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_BUILD=0 SOAK_DURATION_SECONDS=1800 SOAK_INTERVAL_SECONDS=60 scripts/private-testnet-sprint2.sh`
- Sprint 2 isolated seed-host external PoB soak: `PRIVATE_TESTNET_MODE=pob PRIVATE_TESTNET_ROOT=/mnt/HC_Volume_105581636/knodel-external-signoff/knodel-private-testnet PRIVATE_TESTNET_BUILD=0 SOAK_DURATION_SECONDS=300 SOAK_INTERVAL_SECONDS=30 SEED_P2P_PORT=28888 PRODUCER_P2P_PORT=28889 OBSERVER_P2P_PORT=28890 SEED_RPC_PORT=28880 PRODUCER_RPC_PORT=28881 OBSERVER_RPC_PORT=28882 scripts/private-testnet-sprint2.sh`
- Sprint 2 shared/external PoB signoff: `PRODUCER_RPC_URL=http://producer:port/ OBSERVER_RPC_URL=http://observer:port/ EXTERNAL_TESTNET_DURATION_SECONDS=1800 scripts/external-pob-testnet-signoff.sh`
- Public testnet health: `curl -sS https://testnet.koinosfoundation.org/health`
- Public testnet head: `curl -sS https://testnet.koinosfoundation.org/jsonrpc -H 'content-type: application/json' --data '{"jsonrpc":"2.0","id":1,"method":"chain.get_head_info","params":{}}'`
- Public testnet P2P exposure check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/dns4/testnet.koinosfoundation.org/tcp/8888/p2p/QmYV414G6xRzkSUytntEsBsCSjXrVGubfYJn4vpeER2s2W' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=10s SEED_PROBE_PEER_RPC=0 scripts/probe-mainnet-seeds.sh`
- VPS1 legacy observer Peer RPC check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/ip4/46.225.170.6/tcp/28888/p2p/QmeJnbWzRZ91zgTDTxs1UdsbRFFM6B26PntLdk69N63ePY' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=10s scripts/probe-mainnet-seeds.sh`
- VM2 legacy observer Peer RPC check: `GOCACHE=/private/tmp/knodel-go-cache P2P_PEERS='/ip4/46.62.155.105/tcp/28888/p2p/QmXfSaJjSPSivJURC9RrCGKGmKtB3EA3AWEUksY2e189R3' SEED_PROBE_ATTEMPTS=1 SEED_PROBE_TIMEOUT=12s scripts/probe-mainnet-seeds.sh`
- kcli testnet info: `kcli testnet-info`
- kcli configure testnet: `kcli config --network testnet --rpc https://testnet.koinosfoundation.org/jsonrpc`
- kcli config check: `kcli config --show`
- kcli chain check: `kcli chain-info`
- kcli wallet setup: `kcli generate-wallet` or `kcli import-wallet <WIF>`
- kcli faucet instructions: `kcli faucet-info`
- kcli balance: `kcli --network testnet balance`
- kcli transfer through local monolith JSON-RPC: `kcli -r http://127.0.0.1:18122 transfer <address> <amount>`; use `--dry-run` first.
- Live producer regression Level 1: `scripts/live-producer-transaction-regression.sh --level 1 --sample-seconds 0`
- Sprint 5 read-only live producer benchmark: `scripts/benchmark-monolith.py --rpc-url http://127.0.0.1:18122/ --pid-file /Users/pgarcgo/.kcli/knodel-testnet-producer/live-testnet-sync.pid --duration-seconds 300 --interval-seconds 5 --latency-requests 1000 --warmup-requests 20`
- Sprint 5 same-machine native legacy JSON-RPC benchmark: `DURATION_SECONDS=120 INTERVAL_SECONDS=5 LATENCY_REQUESTS=500 WARMUP_REQUESTS=20 scripts/benchmark-legacy-native-jsonrpc.sh`
- Sprint 5 live transaction-submit benchmark: `scripts/benchmark-transaction-submission.js --submit --tx-count 5 --amount 0.001 --max-total-koin 0.01 --confirm-timeout-ms 180000`
- Sprint 5 repeated monolith startup benchmark: `REPORT_DIR=/private/tmp/knodel-monolith-startup-benchmark/sprint5-startup-<date> ITERATIONS=5 START_PORT=28100 scripts/benchmark-monolith-startup.sh`
- Sprint 5 dedicated startup benchmark: `scripts/benchmark-monolith.py --launch-bin node/teleno-node/build/koinos_node --launch-basedir <basedir> --jsonrpc-listen 127.0.0.1:<port> --rpc-url http://127.0.0.1:<port>/ --disable p2p --duration-seconds 10`
- Sprint 5 live P2P sync benchmark: `scripts/benchmark-monolith-sync.py --duration-seconds 120 --sample-interval-seconds 5 --jsonrpc-port 28200 --report-dir /private/tmp/knodel-monolith-sync-benchmark/sprint5-testnet-sync-<date>`
- Sprint 5 thread matrix benchmark: `scripts/benchmark-monolith-thread-matrix.py --duration-seconds 30 --sample-interval-seconds 5 --jsonrpc-port-base 28300 --report-dir /private/tmp/knodel-monolith-thread-matrix/sprint5-thread-matrix-<date>`
- Sprint 5 mixed RPC/submit/sync stress: `scripts/benchmark-monolith-mixed-stress.py --duration-seconds 120 --sample-interval-seconds 5 --sync-jsonrpc-port 28610 --latency-requests 300 --warmup-requests 10 --tx-count 3 --amount 0.001 --max-total-koin 0.005 --confirm-timeout-ms 180000 --submit-transfers --report-dir /private/tmp/knodel-monolith-mixed-stress/live-submit-<date>`
- Sprint 5 offline/replay indexing benchmark: `scripts/benchmark-monolith-index-replay.py --block-count 10000 --batch-size 250 --source-jsonrpc-port 28713 --prep-jsonrpc-port 28714 --replay-jsonrpc-port 28715 --target-blocks-per-second 10000 --report-dir /Volumes/external/knodel-monolith-index-replay-benchmark/sample-10000-<date>`
- Sprint 6.3 package staging smoke: `npm run test:package-staging`
- Sprint 6.3 packaged app smoke: `npm run test:packaged`
- Sprint 6.3 unsigned macOS app package smoke: `npm run package:mac:dir`
- Full live producer performance regression: `RECIPIENT_ADDRESS=<controlled-address> OBSERVER_RPC=<caught-up-observer-rpc> KCLI_PASSWORD_FILE=<0600-password-file> TRANSFER_AMOUNT=0.001 TX_COUNT=5 TX_INTERVAL=0 scripts/live-producer-transaction-regression.sh --level 5 --submit-transfers --sample-seconds 300 --sample-interval 15`
- Live producer transfer smoke dry-run: `RECIPIENT_ADDRESS=<controlled-address> KCLI_PASSWORD_FILE=<0600-password-file> scripts/live-producer-transaction-regression.sh --level 2`
- Live producer transfer smoke with interactive wallet unlock: `RECIPIENT_ADDRESS=<controlled-address> TRANSFER_AMOUNT=0.001 scripts/live-producer-transaction-regression.sh --level 2 --interactive-kcli`
- Live producer transfer smoke with real low-value transfer: `RECIPIENT_ADDRESS=<controlled-address> KCLI_PASSWORD_FILE=<0600-password-file> TRANSFER_AMOUNT=0.001 scripts/live-producer-transaction-regression.sh --level 2 --submit-transfers`
- kcli VHP burn dry-run: `kcli --network testnet burn --dry-run --amount <KOIN_AMOUNT>`
- kcli producer key registration dry-run: `kcli --network testnet register-producer-key --dry-run <producerAddressOrPublicKey> [publicKey]`
- Live testnet prepared producer faucet command: `/faucet 1Kjfrv3qxWvb3afwUdFevZHS1WdT4ginPi`
- Live testnet prepared producer key registration dry-run: `kcli register-producer-key --dry-run AjyRoy9QlZP-AuojYV-cBlHC64mP-ZliaibjmjVnHL97`
- Gate F 5h soak request attempted before seed fix: `SOAK_DURATION_SECONDS=18000 SOAK_INTERVAL_SECONDS=60 JSONRPC_PORT=18082 scripts/soak-mainnet-p2p.sh`; aborted because it produced no real peers.
- Sprint 1.1 backup restore dry-run: `scripts/verify-monolith-backup-restore.sh --dry-run --workdir /Volumes/external/knodel-monolith-restore`
- Sprint 1.1 full backup restore: `scripts/verify-monolith-backup-restore.sh --download --workdir /Volumes/external/knodel-monolith-restore`
- Sprint 4 migration dry-run: `node/teleno-node/tools/migrate_block_store.sh --dry-run --basedir /Volumes/external/knodel-monolith-restore/basedir`
- Sprint 4 migration run with sampled SHA-256 verification: `node/teleno-node/tools/migrate_block_store.sh --basedir /path/to/legacy-basedir --force --verify sample`
- Sprint 4 migration run with full SHA-256 verification: `node/teleno-node/tools/migrate_block_store.sh --basedir /path/to/legacy-basedir --force --verify full`
- Sprint 4 migrated-basedir node smoke: `node/teleno-node/tools/migrate_block_store.sh --verify-node-only --basedir /Volumes/external/knodel-monolith-restore/basedir --jsonrpc-port 18113`
- Sprint 4.3 gRPC monolith smoke: `scripts/compare-grpc-parity.sh 127.0.0.1:<monolith-grpc-port>`
- Sprint 4.3 gRPC legacy parity: `GRPC_PARITY_FAIL_ON_MISMATCH=1 scripts/compare-grpc-parity.sh 127.0.0.1:<monolith-grpc-port> 127.0.0.1:<legacy-grpc-port>`
- Sprint 4.3 gRPC sequential file parity: `GRPC_PARITY_LEGACY_INPUT=/path/to/legacy.json GRPC_PARITY_MONOLITH_INPUT=/path/to/monolith.json scripts/compare-grpc-parity.sh`
- Sprint 4.3 gRPC client validation: `MONOLITH_GRPC_TARGET=127.0.0.1:<grpc-port> MONOLITH_JSONRPC_URL=http://127.0.0.1:<jsonrpc-port> scripts/validate-grpc-client-compatibility.sh`
- Sprint 4.4 fetch pinned upstream integration tests: `scripts/fetch-koinos-integration-tests.sh`
- Sprint 4.4 inventory upstream tests: `scripts/run-koinos-integration-compat.sh inventory`
- Sprint 4.4 Docker legacy baseline for a selected test: `scripts/run-koinos-integration-compat.sh legacy <test-name>`
- Sprint 4.4 native legacy baseline without Docker: `scripts/run-koinos-integration-compat.sh legacy-native <test-name>`; selected tests are `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `transaction_error`, `koin`, `vhp`, `pob`, and `propose_block`.
- Sprint 4.4 monolith compatibility run: `scripts/run-koinos-integration-compat.sh monolith <test-name>`; selected tests are `publish_transaction`, `pending_nonce`, `pending_transaction_limit`, `koin`, `vhp`, and `pob`.
- Sprint 1.2 capture legacy baseline: `python3 scripts/compare-jsonrpc-parity.py write-baseline --url http://127.0.0.1:18084/ --flavor legacy --output /private/tmp/knodel-jsonrpc-legacy-baseline.json`
- Sprint 1.2 compare monolith: `python3 scripts/compare-jsonrpc-parity.py compare-baseline --baseline /private/tmp/knodel-jsonrpc-legacy-baseline.json --url http://127.0.0.1:18083/ --flavor monolith --output /private/tmp/knodel-jsonrpc-parity-comparison.json`
- Sprint 1.3 mempool build/test: `cmake --build node/teleno-node/build --target koinos_node koinos_mempool_adapter_test koinos_block_producer_test --parallel && ctest --test-dir node/teleno-node/build --output-on-failure -R 'koinos_(mempool_adapter|block_producer|gorpc_codec|p2p_one_peer_sync)_test'`
- Frontend/Electron build: `npm run build`

## Next Critical Path

Immediate critical path is now split between implementation progress, mainnet networking diagnostics, and live external testnet validation. Sprint 1.3 is complete locally, and Sprint 2 Phase A private federated validation, Phase B private PoB validation, the 30-minute private PoB soak, isolated seed-host external validation, local `kcli` testnet configuration, the live testnet non-producing observer sync, local live-producer hot-key/basedir preparation, VHP burn, producer-key registration, full live-head catch-up, controlled producer-mode startup, accepted live-testnet PoB block production, a confirmed `kcli transfer` through the local monolith JSON-RPC, Level 3 live transaction regression, the 48h live-producer soak milestone, and full Level 5 live producer regression with independent VPS1 legacy observer witness are complete. Gate F mainnet networking signoff still blocks a mainnet canary, but it does not block continued monolith implementation. The A/B peer acquisition harness is available for future mainnet peer-window classification. Sprint 2.4 should continue as a longer live-producer soak and optional VM2 observer catch-up validation, but the core Level 5 external witness gate is now passed. Sprint 4.3 gRPC compatibility and Sprint 4.4 legacy integration-test compatibility are complete for their selected release-gate suites. Sprint 5 is now active: live monolith, remote legacy observer reference, same-machine native legacy JSON-RPC, live transaction-submit, repeated startup, live P2P sync, offline/replay indexing, code-level RocksDB tuning, thread-pool contention/tuning, Sprint 5.4 error-handling hardening, Sprint 5.5 logging hardening, and the mixed RPC/submit/sync stress gate are captured. Sprint 6.3 packaging readiness has an unsigned macOS `.app` package smoke with staged and packaged-resource verification. The next implementation step is the release UX/readiness checklist: verify the app clearly exposes monolith versus multi-service mode, document end-user migration, and then run signed/notarized DMG release checks when credentials are available. Submit-latency tuning and mainnet Gate F peer-window diagnostics remain explicit follow-up tracks.
