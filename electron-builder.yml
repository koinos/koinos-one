forceCodeSigning: false
appId: io.koinosone.desktop
productName: KoinosOne
directories:
  buildResources: assets/branding
  output: release
files:
  - dist/**
  - dist-electron/**
  - assets/branding/**
  - package.json
mac:
  artifactName: ${productName}-${version}-${arch}.${ext}
  category: public.app-category.developer-tools
  icon: icon.icns
  notarize: true
  target:
    - dmg
dmg:
  artifactName: ${productName}-${version}-${arch}.${ext}
win:
  artifactName: ${productName}-${version}-${arch}.${ext}
  icon: icon.ico
  target:
    - nsis
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
extraResources:
  - from: build/bundle-staging/teleno
    to: teleno
    filter:
      - "**/*"
  - from: build/generated/build-info.json
    to: build-info.json
