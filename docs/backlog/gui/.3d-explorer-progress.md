# 3D Explorer Implementation Progress

Tracks docs/backlog/gui/EXPERIMENTAL_3D_EXPLORER_PLAN.md phases.

- [x] Phase 0 Spike — deps (three 0.169, @react-three/fiber 8.x, drei 9.x for
  React 18), lazy-loaded placeholder scene behind Explorer "3D" subtab with
  Experimental badge, WebGL2 fallback card, pause on hidden/reduced-motion,
  dev FPS probe, EN/ES i18n. Renderer bundle: main chunk ~unchanged (757 kB /
  207 kB gz), lazy chunk Explorer3DView 822 kB / 220 kB gz loaded on demand.
- [x] Phase 1 Data layer — pure lifecycle store src/app/explorer3d.ts (tx
  seen/dropped/included, block ring, caps, prune; 11 unit tests, no three.js
  import), mempool poller hook (2s, only while the 3D view is mounted,
  degrades to blocks-only mode when the RPC lacks the mempool method),
  BlockRow.txIds surfaced from block bodies, HUD shows live pending/blocks
  counts. Bundle: main 758 kB (207 gz), lazy 3D chunk 826 kB (222 gz).
- [x] Phase 2 Static scene — real-data scene replaces the placeholder: API
  gate portal ring, instanced mempool cluster (deterministic per-tx orbital
  slots, 500-instance render cap), chain track of the last real blocks with
  own-producer purple accent, assistant lighting/fog, OrbitControls (drag
  rotate, wheel zoom, slow auto-orbit off under reduced motion). Layout
  helpers (hash01/mempoolSlot) live in the pure module with tests (13 total).
  Bundle: main unchanged, lazy 3D chunk 844 kB / 227 kB gz.
- [x] Phase 3 Life/animation — TxParticles lifecycle animation (spawn from
  the API gate with ease-out, per-particle orbit with bob while pending,
  ease-in absorption into the newest block on inclusion, sink-and-fade on
  drop), block seal flash with pop and smooth slide down the track (damped),
  pulsing gate ring; stronger pulse for own-producer blocks. All animation
  state in refs, instanced matrices updated per frame, frozen (dt=0) under
  reduced motion or hidden window. Bundle: lazy 3D chunk 846 kB / 228 kB gz.
- [ ] Phase 4 Interactivity
- [ ] Phase 5 Polish

## Decisions

- Pinned @react-three/fiber to v8 line (v9 requires React 19; app is on React
  18.3).
- Placeholder scene uses `frameloop="demand"` when hidden or reduced-motion
  instead of unmounting, so the WebGL context survives tab flips.
- `/// <reference types="vite/client" />` added in Explorer3DView for
  `import.meta.env.DEV` (tsconfig does not include vite/client globally).

## Needs user review

- Placeholder scene aesthetics (torus + icosahedron in assistant palette) —
  open Explorer > 3D Explorer and check it feels smooth on the 8 GB Mac (dev
  builds show an fps counter top-left).
